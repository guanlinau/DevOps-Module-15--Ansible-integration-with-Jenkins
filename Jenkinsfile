#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage ("copying files to ansible server") {
            steps {
                script {

                }
            }
        }
    }
}